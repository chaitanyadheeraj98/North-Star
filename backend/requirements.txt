# Adaptive LLM Router - backend runtime dependencies.
# Pinned to minor versions: this is a personal tool that should keep working
# after a month of not being touched.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.9,<3.0
sqlalchemy>=2.0.30,<3.0
pyyaml>=6.0,<7.0
httpx>=0.27,<1.0
